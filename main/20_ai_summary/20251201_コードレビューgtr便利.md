---
title: "コードレビューではgtrが便利！"
source: "https://qiita.com/Nozomuts/items/0f16c7de2a85a7aad6c8"
author:
  - "[[Nozomuts]]"
published: 2025-12-01
created: 2025-12-04
description: "git worktreeをラップしたgtrでレビュー環境を簡単構築。stash不要で快適なレビュー。"
tags:
  - "clippings"
  - "ai_summary"
  - "git"
  - "code-review"
  - "worktree"
  - "cli"
---

## 概要

gtr (Git Worktree Runner)は、`git worktree`をシンプルにラップしたツール。コードレビュー時にstashやブランチ切り替えなしで、独立したレビュー環境を構築できる。

## なぜgtrなのか

- **コマンドがシンプル**: `git gtr new <branch>`だけで作成完了
- **多様なエディタに対応**: VS Code, Cursor, Zedなどを指定して即座に開ける
- **AIエージェントとの連携**: Claude CodeやCodexなどを、そのブランチで独立して起動可能
- **環境構築の自動化**: `npm install`の自動実行や`.env`ファイルのコピーも設定可能

## 導入方法

```bash
git clone https://github.com/coderabbitai/git-worktree-runner.git
cd git-worktree-runner
sudo ln -s "$(pwd)/bin/git-gtr" /usr/local/bin/git-gtr
```

## 基本的な使い方

```bash
# 1. レビュー用ブランチのワークツリーを作成
git gtr new feature/new-ui

# 2. 設定したエディタで開く
git gtr editor feature/new-ui

# 3. CLIベースのコーディングエージェントなどで開く（任意）
git gtr ai feature/new-ui

# 4. サクッと削除
git gtr rm feature/new-ui
```

## 便利な連携設定

- **エディタとAIツールの設定**: `git gtr config set gtr.editor.default code`
- **セットアップ自動化**: `git gtr config add gtr.hook.postCreate "npm install"`
- **ファイルの自動コピー**: `git gtr config add gtr.copy.include "**/.env.example"`

## Tips

`.zshrc`に関数を追加するとさらに便利:

```bash
gtr() {
  git gtr new "$1" && git gtr editor "$1"
}
```

